# Static public site

Scope: `apps/static`, the active public HTML pages and their assets.

## Public content

Keep links, asset paths, and policy content consistent across the static pages.

Treat every page change as public content. Follow the root public-boundary instructions before using examples, logs, screenshots, or information from a conversation.

There is no workspace build step for this directory.
