# Browser extension

Scope: `apps/extension`, the WXT browser extension for UFABC systems.

## Entry points

Choose the entry point that matches the target surface: popup, background, or content script for matrícula, Moodle, or SIGAA.

## External pages

The external UFABC pages are integration targets, not project source.

Keep portal-facing behavior aligned with the canonical domain vocabulary in `CONTEXT.md`.
