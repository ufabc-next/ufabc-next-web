# UFABC Next

This is a pnpm monorepo for the UFABC Next web portal, API, browser extension, and static public pages.

Before editing, read `AGENTS.md`. When working inside a directory with another `AGENTS.md`, read that file first.

For domain behavior, terminology, data models, or business rules, read `CONTEXT.md` and the applicable ADRs.
